{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@11.2.2",
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:app": "pnpm --filter @workspace/cartly run build && pnpm --filter @workspace/api-server run build",
    "start:app": "pnpm --filter @workspace/api-server run start",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
